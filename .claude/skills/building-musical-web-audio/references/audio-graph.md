# Audio graph

Recommended:

```text
voice -> voice gain -> voice filter -> dry bus -> compressor -> master gain -> destination
```

Optional send:

```text
voice -> reverb send -> reverb -> master compressor
```

Keep master gain conservative. Disable expensive effects (convolution
reverb) in low quality mode.
